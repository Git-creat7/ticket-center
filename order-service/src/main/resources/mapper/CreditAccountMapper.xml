<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="asia.creat.mapper.CreditAccountMapper">

    <insert id="add">
        INSERT INTO tb_credit_account (user_id, credits) VALUES (#{userId}, 0)
        ON DUPLICATE KEY UPDATE user_id = user_id
    </insert>

    <select id="selectForUpdate" resultType="asia.creat.entity.CreditAccount">
        SELECT * FROM tb_credit_account WHERE user_id = #{userId} FOR UPDATE
    </select>
</mapper>
