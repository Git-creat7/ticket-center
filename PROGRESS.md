# ticket-center优化记录
---

## 1. 系统现状与架构拓扑

- **当前状态**：微服务架构已全线交付。多机双节点高可用联调通过（Nacos 双实例注册、Gateway 跨主机负载均衡 10/10、单节点下线平滑容灾），测试用例全绿通过。拆分初期的性能退化已全面定位并调优，核心交易链路指标达标。
- **模块拓扑**：
  - `ticket-gateway` (8080)：统一流量入口，基于 Spring Cloud Gateway + Nacos 实现动态路由、负载均衡与内部凭据剥离（防伪造头清理）。
  - `ticket-center-api` (8082)：内容与用户域，承载活动、分类、GEO 检索、评价、点赞、关注与 Feed 流，独立 `ticket_center` 数据库。
  - `order-service` (8083)：交易与履约域，承载票档、库存预扣、秒杀预约、候补排队、订单流转、支付、签到与积分体系，独立 `ticket_order` 数据库 + RabbitMQ。
  - `ticket-common`：全局公共组件，封装统一响应体 `Result`、分布式常量、跨服务 DTO 与会话校验基础拦截器。
  - `ticket-center-web`：Vue 3 + TypeScript + Pinia 前端工程。
  - `benchmark/`：JMeter 压测场景脚本、参数化凭证与全自动账实核对套件。
- **集群与部署**：
  - Docker Compose 编排分层：`docker-compose.middleware.yml`（MySQL 8.4、Redis 7、RabbitMQ 3.13、Nacos 2.4.2）、`docker-compose.init.yml`（MySQL DDL/用户创建与 Nacos 配置导入）、`docker-compose.app.yml`（单机微服务应用全栈）与 `docker-compose.app-prod.yml`（多服务器生产部署）。
  - 双主机部署：支持跨局域网私网 IP 独立连接共享中间件并注册至 Nacos；提供 `deploy/verify-multinode.sh` 完成双实例注册、容器互通与网关负载均衡三档验收。

---

## 2. 核心压测基准与性能调优实录

### 2.1 性能基准对比 (JMeter 实测)

| 场景 | 单体基线 | 微服务拆分初期 (未调优) | 当前微服务优化后 | 关键调优措施 |
| :--- | :--- | :--- | :--- | :--- |
| **签到状态查询** | 2,860 ~ 3,005 QPS<br>60 ~ 65 ms | 3,750 ~ 3,879 QPS<br>42 ms | **3,008 ~ 3,635 QPS**<br>**49 ~ 60 ms** | 整月位图一次拉取 + 内存位运算，Redis 往返从 12 次压至 3 次 |
| **活动详情聚合** | 3,463 ~ 4,499 QPS<br>22 ~ 35 ms | 1,376 ~ 1,528 QPS<br>106 ~ 118 ms | **2,511 ~ 3,584 QPS**<br>**35 ~ 60 ms** | 详情聚合缓存（零 DB 穿透）；剥离 Feign 票档跨服务查询；UV 想看人数动静分离 |
| **高并发秒杀预约** | 1,012 ~ 1,101 QPS<br>41 ~ 54 ms | 223 ~ 243 QPS<br>357 ~ 382 ms | **728 ~ 731 QPS**<br>**97 ~ 101 ms** | Lua 预扣前置快速失败，被拒请求不再排行锁；事务提交后即时推送建单任务 (出票延时 ~250ms) |

### 2.2 关键性能调优实战

#### 1. 秒杀预约削峰与行锁竞争消除 (223 → 728+ QPS)
- **痛点诊断**：拆分初期，无效与售罄请求依然进入数据库事务排队抢占票档行锁（`FOR UPDATE`），持锁期间叠加 Feign 远程调用与 Redis 操作，高并发下大量线程阻塞互等，QPS 跌至 223 ~ 243，平均延迟 357 ~ 382 ms。
- **优化方案**：
  1. **Lua 原子预扣前置**：将库存与资格判断前置到受理入口最前端。售罄请求在 Redis 内存层毫秒级被拦截，只有放行请求才进入后续流程；
  2. **出票由轮询改即时触发**：在本地事务提交后（`afterCommit`）即时异步推送任务，端到端出票延迟从 1s+ 缩减至 225 ~ 278 ms；
  3. **孤儿清理移出高频路径与宽限期拉长**：将崩溃孤儿清理（`releaseOrphans`）移出候补行锁路径，收敛至启动时执行；候选集改用 Set 消除 $O(n^2)$ 匹配；宽限期由 60 秒延长至 10 分钟，杜绝开售瞬时排队在途请求被误杀回滚；
  4. **即时推送线程池有界化防 OOM**：即时推送改为固定 4 线程 + 1000 有界队列 + DiscardPolicy 丢弃策略，队列满时丢弃交由每秒定时扫描兜底，消除无界队列内存耗尽风险。
- **收益**：秒杀吞吐提升 **3.3 倍**，平均延迟下降 **75%**。

#### 2. 活动详情聚合与动静分离 (1,376 → 2,511 ~ 3,584 QPS)
- **痛点诊断**：拆分前单体只缓存了 `Event` 实体，仍穿透查分类和票档列表；微服务拆分后，详情接口命中缓存后仍通过 Feign 跨服务同步调用 `order-service` 获取票档，叠加 UV 计算，单请求经历 4 次网络 RTT，耗时高达 110ms+。
- **优化方案**：
  1. **聚合视图缓存**：缓存对象改为完整的 `EventDetailVO`，直接命中 0 次 MySQL 查询；
  2. **前后端动静解耦**：将票档列表从活动详情页拆离，前端并行异步拉取；将 UV 想看人数剥离为独立响应式状态接口，长效静态内容缓存与实时变动计数彻底解耦。
- **收益**：详情接口 QPS 回升至 2,511 ~ 3,584，消除跨服务强耦合。

#### 3. 签到位图 12 次 RTT 压缩至 3 次 (1,206 → 3,374 QPS)
- **痛点诊断**：签到状态接口最多只读 2 个 Key，但原代码按位逐次请求 Redis（今日 `GETBIT`、连签 `BITFIELD`、当月 `BITCOUNT`、本周 7 天逐次 `GETBIT`），叠加拦截器单请求产生 **12 次 Redis 往返**。在 200 并发压测下，连接池等待导致单次往返边际耗时超 11ms。
- **优化方案**：
  - 改用 `RedisCallback` 直接获取整月原始字节数组（最多 4 字节）；
  - 在 JVM 内存中基于高位在前语义执行位运算（`0x80 >>> bit`）和 `Integer.bitCount`；
  - 往返次数从 12 次压缩为 **3 次**（2 次鉴权拦截器 + 1 次位图拉取）。
- **收益**：QPS 从 1,206 暴涨至 **3,374（2.8 倍）**，时延下降 **73%**。

---

## 3. 核心一致性与高可靠架构

### 3.1 资金安全与防资损（快照原则）
- **成交价与抵扣额固化**：`tb_ticket_order` 显式增加 `used_credits`（抵扣积分）与 `price`（成交单价）。严禁在取消订单时根据当前票价反推抵扣额，杜绝运营中途改价导致的退款资损。
- **预约到落单价格冻结**：秒杀预约成功时将当时票价作为快照序列化进 MQ 消息体，消费者落库严格认快照单价，阻断因 MQ 积压或消费延迟引发的价格漂移。

### 3.2 高并发状态机与原子流转 (消除 Check-Then-Act)
- **Redis 原子返回值驱动业务流转**：
  - **签到防重**：利用 `SETBIT` 返回旧值的特性，仅当旧值为 0 时确认当天首次签到并增加积分，杜绝并发连击导致的积分超发；
  - **点赞防重**：利用 `ZREM` / `ZADD` 返回值确认集合元素是否真正产生变动，仅在成功变更时同步更新 MySQL 计数。
- **防超卖与一人一票双层护城河**：
  - 第一层（Redis）：Lua 脚本原子执行资格 Set 检查与库存自减，拦截绝大多数无效流量；
  - 第二层（MySQL）：通过 `stock >= #{quantity}` 条件更新防止库存为负，通过唯一索引 `uk_user_ticket_active` 实现终极幂等兜底。

### 3.3 分布式补偿与可靠消息链路
- **任务表 + 提交后推送**（Outbox 的简化形态，没有独立 relay）：
  - 预约受理时，在同一本地事务内记录 `tb_reservation_task`；
  - 事务提交后通过 Spring 事务同步机制（`afterCommit`）即时异步触发通知出票；
  - 兜底定时任务以固定周期拉取未完成任务（配置 Redisson 分布式锁与 `limit 500` 流控），保证在进程挂掉或网络丢包下最终一致。
- **对称补偿与边界保护**：
  - **对称回滚准则**：Confirm 失败回调与死信队列消费者统一采用 `rollbackIfNotPersisted` 策略——必须先查库确认订单未创建才允许回滚 Redis 库存与资格 Set，防止因网络抖动误回滚引发超卖；
  - **事务边界解耦**：订单取消与库存回补中，Redis 库存释放挂在 MySQL 事务提交成功之后（`afterCommit`），避免因 Redis 提前释放成功而后置 SQL 异常回滚造成的永久脏数据。

### 3.4 缓存治理与全链路 N+1 消除
- **防脏写提交后失效**：详情聚合缓存的失效操作挂在 `afterCommit` 执行，防止事务尚未提交前并发读请求将旧脏数据重新回写至 Redis。
- **N+1 查询全景治理**：
  - 订单列表：抽取票档 ID 与活动 ID 集合，两轮 `selectBatchIds` 批量加载并在内存组装；
  - 评价列表：作者信息批量 `listByIds` 查库，点赞状态采用 Redis Pipeline 批量判定；
  - 演出列表：复用带有全量 Redis 缓存的分类字典服务，消除逐条查库。

### 3.5 容器化跨进程时区显式契约
- **故障排查**：容器化部署后新订单常态化被秒级取消。根因为 MySQL 容器时区默认 UTC，应用基于北京时间（CST），驱动在传递无时区 `LocalDateTime` 时不做时区转换，导致数据库比较时新订单全被判定为“8小时前创建”。
- **工程契约**：容器编排统一注入 `TZ: Asia/Shanghai`；对跨进程时间比较，优先采用绝对时刻或在数据库端统一完成比较表达式计算。

---

## 4. 核心工程法则与高频踩坑沉淀 (Key Takeaways)

1. **订单是快照，不是外键的视图**：凡是下单那一刻的事实（成交价、抵扣额、优惠明细），必须固化在订单行上。关联表数据会发生漂移，订单历史绝不可漂移。
2. **命中路径看 I/O 次数，不看是否调了 Redis**：只要命中路径上依然穿透 MySQL，性能下限就由慢速磁盘和网络 RTT 决定。动静分离剥离高频动态计数，是保住聚合缓存高吞吐的前提。
3. **“检查后写入”(Check-Then-Act) 是并发漏洞的温床**：优先利用 `SETBIT`、`ZADD`、`SETNX` 等原子命令自身返回值决定后续动作，杜绝并发间隙穿透。
4. **兜底任务往往在系统最脆弱时引爆**：平时数据为空的定时兜底任务极具欺骗性，一旦主链路故障就会瞬时承压。兜底任务必须自带分布式锁与有界限流（`LIMIT`）。
5. **永远不要把外部写操作圈进数据库事务**：Redis 写入与 MQ 投递不参与数据库本地事务回滚。外部副作用操作必须挂在事务提交之后（`afterCommit`）执行，或引入 Outbox 表实现两阶段解耦。
6. **没有噪声基准的压测产出的是故事**：判断性能差异前，先测出同配置下的内生极差；统计脚本必须防御除零和分母钉死漏洞。
7. **永远不要保留不可能失败的测试断言**：断言必须经受得住反问——“什么样的破坏性输入能让它变红”。两边皆为 null 的恒等判定是造成假绿的最大陷阱。
